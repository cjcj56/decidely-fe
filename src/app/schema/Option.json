{
    "$schema": "http://www.decidely.com/schemas",
    "$id": "http://www.decidely.com/schemas/Option.json",
    "title": "Option",
    "type": "object",
    "properties": {
        "id": { "type": "integer" },
        "text": { "type": "string" },
        "priority": { "type": "integer" },
        "decision": { "type": { "$ref": "Decision.json#/" } }
    },
    "required": ["id", "text", "priority", "decision"],
    "additionalProperties": false
}
